interface:
  display_name: "DocenteDocAi"
  short_description: "Repo-specific patterns and workflows for DocenteDocAi"
  default_prompt: "Use the DocenteDocAi repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true