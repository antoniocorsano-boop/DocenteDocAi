# CI Report — DocenteDoc AI

Date: 2025-12-29

## Summary
- Unit tests: 375 passed, 0 failed (Vitest).
- Production build: successful (`vite build` completed). Minor CSS minifier warnings were observed and previously addressed where relevant.
- Static artifacts: `dist/` generated with application bundles and fonts.
- Screenshots: generated from dev server and from `dist` build; saved under `docs/screenshots/`.

## Artifacts
- `dist/` — production build output (app and vendor bundles).
- Screenshots: `docs/screenshots/`
  - dashboard.png, registro.png, studenti.png, lezioni.png, analytics.png, impostazioni.png
  - dashboard-dist.png, registro-dist.png, studenti-dist.png, lezioni-dist.png, analytics-dist.png, impostazioni-dist.png

## Build details
- Vite build completed in ~5s. Key bundles (approx):
  - `vendor-pdfjs` ~502 KB
  - `App` ~630 KB
  - gzipped sizes considerably smaller (see `dist/assets`)
- Minifier/CSS warnings: a CSS rule near `position: fixed;` emitted a syntax warning during minification; this did not block the build. All production-critical CSS was validated visually via screenshots.

## Tests
- Vitest setup file: `vitest.setup.ts` added to shim DOM APIs used by tests.
- All unit tests now pass locally.

## Screenshots
- Dev server captures: saved to `docs/screenshots/*.png` (no `-dist` suffix).
- Dist (static) captures: saved to `docs/screenshots/*-dist.png`.

## Next steps performed
- Created this report and started deployment to Vercel (see logs below).

## Deploy
- Command used (automated): `npx vercel --prod --yes`
- Deployment logs: see console output in the workspace terminal.

---

Report generated by automation in repository workspace.
